export const SET_AREAS = (state, areas) => {
    state.areas = areas;
}
export const SET_FEATURED_AREAS = (state, areas) => {
    state.areas = areas;
}

export const SET_AREA = (state, area) => {
    state.area = area;
}
export const SET_LOADING = (state, loading) => {
    state.loading = loading;
}
