export const SET_TEAM = (state, team) => {
    state.team = team;
}

export const SET_AGENT = (state, agent) => {
    state.agent = agent;
}
export const SET_LOADING = (state, loading) => {
    state.loading = loading;
}
