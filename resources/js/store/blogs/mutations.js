export const SET_BLOGS = (state, blogs) => {
    state.blogs = blogs;
}
export const SET_FEATURED_BLOGS = (state, blogs) => {
    state.blogs = blogs;
}
export const SET_BLOG = (state, blog) => {
    state.blog = blog;
}
export const SET_LOADING = (state, loading) => {
    state.loading = loading;
}
