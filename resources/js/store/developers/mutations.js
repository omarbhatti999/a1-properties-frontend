export const SET_DEVELOPERS = (state, developers) => {
    state.developers = developers;
}

export const SET_DEVELOPER = (state, developer) => {
    state.developer = developer;
}
export const SET_LOADING = (state, loading) => {
    state.loading = loading;
}
